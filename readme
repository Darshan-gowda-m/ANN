sudo apt update
sudo apt install python3-matplotlib python3-pil -y
# Move to project directory
cd /mnt/c/"Users/DHARSHAN GWODA M/OneDrive/UVCE/6th/ml/face_recognition/code"

# Optional rebuild
make clean && make

# 🔵 Train 1: shades1.net → Display & save output
./facetrain -n shades1.net -t ../trainset/straightrnd_train.list \
-1 ../trainset/straightrnd_test1.list -2 ../trainset/straightrnd_test2.list -e 75 | tee train_log_shades1.txt

# 🟢 Train 2: shades2.net → Display & save output
./facetrain -n shades2.net -t ../trainset/straighteven_train.list \
-1 ../trainset/straighteven_test1.list -2 ../trainset/straighteven_test2.list -e 75 | tee train_log_shades2.txt

# 🔴 Train 3: pose.net → Display & save output
./facetrain -n pose.net -t ../trainset/all_train.list \
-1 ../trainset/all_test1.list -2 ../trainset/all_test2.list -e 100 | tee train_log_pose.txt


python3 v1.py
